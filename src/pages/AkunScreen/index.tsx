import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const AkunScreen = (props: Props) => {
  return (
    <View>
      <Text>AkunScreen</Text>
    </View>
  );
};

export default AkunScreen;

const styles = StyleSheet.create({});
